extends layout

block content
  form(method="POST" action='/dishes/add/comentarios/' +dishe.id)
    <div class="form-group">
      <label for"nombre">Ratings</label>
      input(type='number' class="form-control" id="nombre" name="ratings" placeholder="5")
    </div>
    <div class="form-group">
      <label for="comentario">Comentario</label>
      input(type="text" name="comment" id="imagen" class="form-control" placeholder="que bueno")
    </div>
    <div class="form-group">
      <label for="comentario">Autor</label>
      input(type="text" name="author" id="imagen" class="form-control" )
    </div>
      <input type="submit" value="Enviar">
  </form>
